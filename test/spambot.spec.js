'use strict'

var assert = require('chai').assert;
var Logger = require('../src/utils/logger');

describe('Test `spambot` command line functionality', function() {

  it('Should be able to get a list of proxies with command line', function() {

  });

  it('Should be able to search and get a list of article urls with command line', function() {

  });

  it('Should be able to send a spam comment with command line', function() {

  });

});
